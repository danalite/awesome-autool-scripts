task: win32-sysinfo

# https://www.educba.com/useful-powershell-scripts/
actions:

  # BIOS information
  - os.shell(Get-CimInstance -ClassName Win32_BIOS) => $txt

  # Get IpV4 Address
  - os.shell(Get-NetIPAddress -AddressFamily IPv4) => $txt