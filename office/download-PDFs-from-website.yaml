---
 task: download-PDFs-from-website

 actions:
    - web.init({{ {'profile':'default','headless':True} }})
    - web.open(https://www.csl.cornell.edu/courses/ece5775/)

    # Find all "a" tags with "href" attribute containing "pdf"
    - web.find( //a[contains(@href\, '.pdf')] ) => $elems
  
    - cmd.for_each($elems) => $index, $elem:
      - web.get($elem, href) => $url
      - cmd.if( {{ 'lecture' in $url }} ):
        - web.download($link, /Users/hecmay/Desktop/)
