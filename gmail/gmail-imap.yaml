---
 task: gmail-imap
 desc: monitor gmail accounts emails using IMAP

 actions:
   # your gmail account is treated as a database of emails
   - db.init(my@gmail.com, {{ {'password':$env[myIdPassword]} }}) => $imap

   - db.read($imap, {{ {'from':'bestbuy.com'} }}) => $emails

