task: copy-excel-table
actions:
  - cmd.for_each( $env[CLIPBOARD] ) => $index, $line:
      - data.read({{ $line.split('\t') }}) => $items
      - key.type({{ $items[0] }}, Tab)
      - key.type({{ $items[1] }}, Tab+Tab)
configs:
  hotkey: ''
  options: []
  shortcut: false
