task: paddle-ocr
actions:
  # let user select a region
  - os.shell(screencapture -c -i -x)
  
  # Save the screenshot to /tmp/screencapture.png

  - os.shell(sips -s format png /tmp/screencapture.png --out /tmp/screencapture.png)
  
  # Run OCR on the screenshot

  # save result to clipboard
  


configs:
  options: []
  hotkey: ''
  shortcut: false
