
task: web-service-listener
actions: 
  
  # web server listener on the background
  - event.on(__RETAIL_MONITOR__, {{  {'target':'smzdm.com', 'interval':3600} }}) => $deals:
    - cmd.print($deals)
    
  - cmd.sleep(100000s)

