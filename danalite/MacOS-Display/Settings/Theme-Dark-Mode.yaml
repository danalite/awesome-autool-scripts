task: switch-dark-mode
actions:
  - >-
    os.shell(osascript -e 'tell app "System Events" to tell appearance
    preferences to set dark mode to not dark mode')
configs:
  options: []
  shortcut: false
