task: random-picture
actions:
  - >-
    user.input({{ {'type':'select', 'title':'select apps to
    close','options':['Akk'], 'preset':True} }}) => $target
    
  # pc-girl wallpaper
  - web.http(GET, https://v.api.aa1.cn/api/pc-girl_bz/index.php?wpon=json) => $resp
  - data.read({{ $resp[img] }}) => $url

  - os.shell(open -a 'Google Chrome' 'http:{{ $url }}')

configs:
  hotkey: ''
  options: []
  shortcut: true
