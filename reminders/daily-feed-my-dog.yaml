---
 task: activity-feed-my-dog

 configs:
    - options: ['remote']

 actions:
   # Timer to remind my roommate to feed my dog
   - event.on(__TIMER, 10:00am EST):
      - user.notify($env[ROOMMATE1], {{ {'title':'DOG's breakfast', 'content':'dog is hungry'} }})

   # Set a timer to remind myself to walk my dog
   - event.on(__TIMER, 6:00pm EST):
      - user.notify($env[MY_IPHONE], {{ {'title':'waling DOG', 'content':'Your dog wants to play'} }})
  
   # Timer to remind my dog's medication
   - event.on(__TIMER, 6:00pm EST):
      - user.notify($env[MY_IPHONE], {{ {'title':'walking DOG', 'content':'Time to take flea & tick medicines'} }})